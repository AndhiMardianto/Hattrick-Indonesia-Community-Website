<nav class="navbar" aria-label="Navigasi Utama">
  <div class="container nav-wrapper">
    <ul class="nav-links">
      <li><a href="/">Beranda</a></li>
      <li><a href="/tentang-kami">Tentang Kami</a></li>
    </ul>
  </div>
</nav>

<style>
  .navbar { 
    border-bottom: 1px solid var(--color-border); 
    background: #ffffff; 
    /* Padding vertikal agar pembungkus putihnya proporsional */
    padding: 1.25rem 0; 
    /* Pengaman struktur navbar */
    position: relative;
    z-index: 10;
  }
  
  .nav-wrapper { 
    display: flex; 
    /* Membuat konten di dalamnya (nav-links) berada tepat di tengah-tengah */
    justify-content: center; 
    align-items: center; 
  }
  
  .nav-links { 
    display: flex; 
    list-style: none; 
    /* Jarak antar tombol (Beranda dan Tentang Kami) */
    gap: 1rem; 
    margin: 0;
    padding: 0;
    align-items: center; 
  }
  
  /* Bentuk Tombol Hijau Lapangan Bola yang Ramping */
  .nav-links a { 
    text-decoration: none; 
    color: #ffffff; 
    background-color: #15803d; 
    font-weight: 700; 
    padding: 0.4rem 1.25rem; 
    border-radius: 0.5rem; 
    display: inline-block;
    line-height: 1.2;
    transition: background-color 0.2s ease;
  }
  
  /* Efek interaktif saat kursor menyentuh tombol */
  .nav-links a:hover { 
    background-color: #16a34a; 
    color: #ffffff;
  }

  /* Khusus Layar HP Sangat Kecil (Layar di bawah 360px agar tetap aman) */
  @media (max-width: 360px) {
    .nav-links {
      gap: 0.5rem; /* Memperkecil sedikit jarak antar tombol agar tidak sesak */
    }
    .nav-links a {
      padding: 0.4rem 1rem; /* Mengurangi sedikit padding tombol di HP jadul */
      font-size: 0.95rem;
    }
  }
</style>